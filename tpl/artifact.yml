- name: gflow
  version: v{{VERSION}}
  origin_addr:
    url: "{{ART_REPO}}/v{{VERSION}}/galaxy-flow-v{{VERSION}}-{{RUN_TARGET}}.tar.gz"
  cache_enable: false  
  cache_addr :
    url: "{{ART_CACHE_REPO}}"
  local: galaxy-flow.tar.gz
