- name: gflow
  version: v0.11.0
  origin_addr:
    url: "{{ART_REPO}}/${USED_VER}/galaxy-flow-${USED_VER}-x86_64-unknown-linux-gnu.tar.gz"
  cache_enable: false  
  cache_addr :
    url: "{{ART_CACHE_REPO}}"
  local: galaxy-flow-${USED_VER}.tar.gz
